name: Schedule Test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:  # Allows manual triggering from GitHub Actions tab

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Scheduled run fired" && date
